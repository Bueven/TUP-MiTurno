import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.miturno.web',
  appName: 'Mi Turno Web',
  webDir: 'dist/mi-turno-web/browser'
};

export default config;
